import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { ColorPalette, ColorItem } from '@storybook/addon-docs';

<Meta title="Design System/Tokens" />

# Neural Pulse Design Tokens

Design tokens are the visual design atoms of the design system — specifically, they are named entities that store visual design attributes. We use them in place of hard-coded values to maintain a scalable and consistent visual system for UI development.

## Colors

Color tokens are used to ensure consistent color usage across all components and layouts.


## Typography

Typography tokens are used to keep font styles consistent and accessible.


## Shadows

Shadow tokens are used to create depth in components and interfaces.


## Borders

Here's how to apply border-radius tokens to UI elements.
